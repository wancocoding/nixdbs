{
    "packages": {
        "neovim": {
            "default": { "name": "nvim", "method": "brew" }
        },
        "ninja": {
            "default": { "name": "ninja", "method": "brew" },
            "arch": { "name": "ninja", "method": "system" },
            "ubuntu": { "name": "ninja-build", "method": "system" },
            "fedora": { "name": "ninja-build", "method": "system" },
            "gentoo": { "name": "dev-util/ninja", "method": "system" },
            "opensuse": { "name": "ninja", "method": "system" },
            "alpine": { "name": "ninja", "method": "system" },
            "freebsd": { "name": "ninja", "method": "system" }
        },
        "pyenv": {
            "default": { "name": "pyenv", "method": "manual" }
        }
    },
    "commands": {
        "install": {
            "default": "brew install",
            "arch": "sudo pacman -Syu",
            "ubuntu": "sudo apt install -y",
            "fedora": "sudo dnf install",
            "gentoo": "sudo emerge",
            "opensuse": "sido zypper in",
            "alpine": "sudo apk add",
            "freebsd": "sudo pkg install"
        }
    }
}
